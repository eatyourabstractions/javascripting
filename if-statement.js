let fruit = 'orange'
let me = 'The fruit name has'
let ifTrue = `${me} more than five characters.`
let ifFalse = `${me} five characters or less.`

if(fruit.length > 5){
    console.log(ifTrue)
} else {
    console.log(ifFalse)
}